{{ signature }}
    for (int64_t i = 0; i < {{ m }}; ++i) {
        for (int64_t j = 0; j < {{ n }}; ++j) {
            {{ acc_type }} acc = {{ acc_init }};
            for (int64_t t = 0; t < {{ k }}; ++t) {
                acc += {{ mat1_access }} * {{ mat2_access }};
            }
            {{ out_access }} = ({{ beta }}) * {{ input_access }} + ({{ alpha }}) * acc;
        }
    }
}
